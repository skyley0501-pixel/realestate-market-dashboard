

# Supabase# Transaction pooler
DATABASE_URL=<project-ref>:<password><region>

# Session pooler / Direct connection
DIRECT_URL=<project-ref>:<password><region>

REINFOLIB_API_KEY=

# Google AI Studio（https://aistudio.google.com/apikey）で発行。カード登録不要。
# 注意: このAPIキーのプロジェクトにGoogle Cloud ConsoleでBillingアカウントを紐付けないこと（紐付けると無料枠を超えた分から課金される）。
# 使用モデルはFlash/Flash-Lite系（無料枠）のみに限定すること。詳細はdocs/design.md 7.2節参照。
GEMINI_API_KEY=