@AGENTS.md

## このプロジェクトの実装ルール

- 小さな単位で実装する（1PR = 1つの動作確認できる変更）
- 実装のたびに動作確認方法を示す（コマンド・URL・期待される結果）
- エラーが起きた場合は原因と修正方法を明記する
- 変更内容に応じて README.md を更新する
- コメントは最低限（「なぜ」が非自明な箇所のみ）
- TypeScript の型安全を重視し、`any` は使用しない（ESLintで禁止済み）
- ESLintエラーは常に0を維持する（`npm run lint`）
- DRY・SOLID原則を意識し、責務が単一になるよう分割する
- Server Componentsを優先し、インタラクションが必要な部分のみ Client Component にする
- アクセシビリティ対応（コントラスト・キーボード操作・aria属性）を行う
- レスポンシブ対応（モバイル〜デスクトップ）を行う
- 設計判断は「なぜその設計にしたか」を必ず説明する
- 詳細な設計方針は `docs/design.md`、要件は `docs/requirements.md`、実装順序は `docs/roadmap.md` を参照する
